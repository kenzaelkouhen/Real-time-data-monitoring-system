
#Configuration of prometheus to scrape metrics from exporter


# A scrape configuration containing exactly one endpoint to scrape (raspar):


scrape_configs:

- job_name: prometheus

  scrape_interval: 5s

  static_configs:

  - targets:
    - prometheus:9090
    - cadvisor:8080


  
